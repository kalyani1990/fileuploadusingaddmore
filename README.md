# fileuploadusingaddmore
The addmore button click will add more fields into the database

Create a database 'crc'.
create table 'internship' and the columns are company, project, duration, key_learning.

